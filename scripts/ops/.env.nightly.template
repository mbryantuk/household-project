# Totem Nightly Suite Credentials Template
# 1. Copy this file to .env.nightly: cp .env.nightly.template .env.nightly
# 2. Fill in your Google App Password (not your regular Gmail password)
# 3. NEVER commit .env.nightly to git (it is already in .gitignore)

SMTP_USER=mbryantuk@gmail.com
SMTP_PASS=kldq otxk yjrf likhe
REPORT_EMAIL=mbryantuk@gmail.com

# Optional Overrides
 SMTP_HOST=smtp.gmail.com
 SMTP_PORT=465
